version: 2
jobs:
  build-docs:
    docker:
      - image: circleci/python:3.7.0
    environment:
      - COMMIT_AUTHOR_EMAIL: "guiadco@geekhomeinside.com"
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "be:c0:06:e5:f0:c8:14:8b:1d:dd:39:c0:15:86:17:8c"
      - run:
          name: Install Virtualenv dependencies for Pip
          command: pip install --user virtualenv
      - run:
          name: Prepare shell commands
          command: chmod +x .circleci/deploy-ghpages.sh
      - deploy:
          name: Deploy
          command: PATH=$PATH:/home/circleci/.local/bin/ .circleci/deploy-ghpages.sh docs

  create-release:
    machine: true
    environment:
      - COMMIT_AUTHOR_EMAIL: "guiadco@geekhomeinside.com"
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: curl -L https://github.com/stedolan/jq/releases/download/jq-1.5/jq-linux64 -o jq && chmod +x jq && sudo mv jq /usr/bin
      - deploy:
          name: Generate release notes
          command: .circleci/release_notes.sh

  ansible-lint:
    docker:
      - image: python:2.7
    working_directory: ~/repo
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "be:c0:06:e5:f0:c8:14:8b:1d:dd:39:c0:15:86:17:8c"
      - run:
          name: Prepare shell commands
          command: chmod +x ./.circleci/lint.sh
      - run:
          name: Lint playbooks and roles
          command: ./.circleci/lint.sh

  test-install:
    machine: true
    working_directory: ~/repo
    steps:
      - add_ssh_keys:
          fingerprints:
            - "be:c0:06:e5:f0:c8:14:8b:1d:dd:39:c0:15:86:17:8c"
      - run:
          name: Update Cache
          command: sudo apt-get -y update
      - run:
          name: Add Ansible PPA
          command: sudo apt-add-repository ppa:ansible/ansible
      - run:
          name: Clone the repository
          command: git clone https://github.com/guiadco/geekhomeinside.git .
      - run:
          name: Update Cache
          command: sudo apt-get -y update
      - run:
          name: Move to your current BRANCH
          command: git checkout $CIRCLE_BRANCH
      - run:
          name: Install Ansible latest version
          command: sudo apt-get install -y ansible
      - run:
          name: Run Allspark Playbook with verbose mode
          command: ansible-playbook -i ./.circleci/hosts.circleci install.yml -vvv
      - run:
          name: See your container's install
          command: docker ps -a
      - run:
          name: See your volume's install
          command: docker volume ls

workflows:
  version: 2
  test-to-master:
    jobs:
      - ansible-lint
      - test-install
      - build-docs
      - create-release:
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /[0-9]+(\.[0-9]+)*/
